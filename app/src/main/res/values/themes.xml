<resources>
    <!-- Base application theme. -->
    <style name="Theme.StromRein" parent="Base.Theme.StromRein" />

    <style name="Base.Theme.StromRein" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/primary_container_light</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container_light</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/secondary_light</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/secondary_container_light</item>
        <item name="colorOnSecondaryContainer">@color/on_secondary_container_light</item>

        <!-- Surface -->
        <item name="android:colorBackground">@color/neutral_50</item>
        <item name="colorSurface">@color/neutral_100</item>

        <!-- Error -->
        <item name="colorError">@color/red</item>
        <item name="colorOnError">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <!-- Shapes -->
        <item name="shapeAppearanceCornerExtraSmall">@style/ShapeAppearance.StromRein.Corner.ExtraSmall</item>
        <item name="shapeAppearanceCornerSmall">@style/ShapeAppearance.StromRein.Corner.Small</item>

        <!-- Toolbar -->
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar.Surface</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowLightNavigationBar">?attr/isLightTheme</item>

        <!-- Extra -->
        <item name="elevationOverlayEnabled">false</item>
    </style>

    <string-array name="themes">
        <item>@string/theme_system</item>
        <item>@string/theme_light</item>
        <item>@string/theme_dark</item>
    </string-array>

    <string-array name="themes_value">
        <item>system</item>
        <item>light</item>
        <item>dark</item>
    </string-array>
</resources>